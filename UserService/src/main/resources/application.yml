server:
  port: 8088

spring:
  application:
    name: user-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ProfileDB
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      healthcheck:
        enabled: true
  instance:
    prefer-ip-address: true

